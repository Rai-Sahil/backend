EVENT SOURCING

Instead of storing the actual data in database, events are stored as sequence and the current state is rebuilt by replaying these events in order. Instead of updating DB records directly, each change is recorded as an immutable event.

Event storage example

Bank DB. Instead of storing current balance and changing them on deposit/withdrawal, we can store events. We can replay these events to get Account Balance. 
	•	AccountOpened { accountID: 123, initialBalance: 0 };
	•	MoneyDeposited { accountID: 123, amount: 100 }
	•	MoneyWithdrawn { accountID: 123, amount: 50 }

New York Times. They have stored each article, it’s changes in event. So, if anyone wants to have a look at older version, they can show that. Otherwise just replay all these events and make something. 
	•	ArticleCreated
	•	HeadlineUpdated
	•	ImageAdded
	•	BylineUpdated

CDC (Change Data Capture)
CDC tools (like Debezium) watch your database and generates events every time something changes. Example:-
	•	Let’s say you have made an update to user tables
	⁃	Update users SET email = “newexample@gmail.com” WHERE id = 1;
	•	CDC captures this as an event
	⁃	{
  		     "type": "UserEmailUpdated",
               "userId": 1,
               "newEmail": "new@example.com",
               "timestamp": "2025-04-07T08:00:00Z"
          }
	•	This event can be other services (like notifying users, or logging the changes) without directly touching the database again. 

Outbox Pattern
Instead of directly sending a message to another system as part of a database operation (e.g., during a CREATE, UPDATE, or DELETE action), the Outbox Pattern stores the message in the same transactional context as the database operation. A separate process or service then reads the messages from the outbox and sends them to the message broker. Example Workflow:
	1.	Write to outbox in a transaction
	⁃	When a new order is created, the system creates two records in one transaction:
	⁃	One in the orders table for the new order. 
	⁃	One in the outbox table for the event OrderCreated (including details about the order).
	⁃	Ensures both orders are saved together, if system crashes both the transactions are cancelled. 
	2.	Poll the outbox and send events:
	⁃	A background worker or polling service continuously checks the outbox table for unsent events.
	⁃	The service reads an unsent event, sends it to the message broker, and then marks it as "sent" by updating the sent_at field.
	3.	Message broker delivery
	⁃	The event is sent to the message broker (like Kafka, RabbitMQ), where other services (e.g., inventory, shipping) can consume the event and act accordingly.

Microservices
Adding an item into the cart is recorded as an event. Actions recorded as an event:-
	•	ItemAddedToCart { userId: 101, itemId: 432, quantity: 2 }
	•	ItemRemovedFromCart { userId: 101, itemId: 432 }
	•	CartCheckout { userId: 101, cartId: 77 }
This system can also store timeline of what happened and it will help other services with:-
	•	InventoryService: Knows what item to reduce and how much to reduce
	•	BillingService: Knows what to charge

Event sourcing could also be used for more examples, such as :- 
	1.	Ticket Booking System
	2.	Invoice Service
	3.	Learning Management System (CourseEnrolled, LessonLearned, QuizSubmitted, …)
	4.	Logistics and Fleet Management
	5.	Game backend


WHEN TO USE EVENT SOURCING
Event sourcing isn’t always the right default, even if it seems cool and powerful. Here’s a breakdown of when to use event sourcing:-
	•	You need full history and audibility
	⁃	Healthcare (patient records, prescriptions)
	⁃	Finance (transactions, payment)
	⁃	Legal/Insurance (claims, policy changes)
	•	Domain is highly collaborative or Stateful
	⁃	Google docs, overleaf, game servers, figma
	⁃	As we need to track every change here.
	•	You need to rebuilt the state on demand
	⁃	If system’s state evolve or change often, and you want to be able to replay events with new logic.
	⁃	Change how balance or tax is calculated — just reprocess the event with new rules. 
	•	You have Event-Driven Microservices
	⁃	Events can be used to notify other services (CQRS pattern)
	⁃	Good for decoupling and async processing, and scaling write vs read paths.

WHEN NOT TO USE EVENT SOURCING
	•	Simplicity is better
	⁃	If all you need is basic CRUD app, event sourcing is overkill.
	⁃	A portal that just displays some data may not need this complexity.
	•	No need for history
	⁃	If we don’t need a audit or traceback
	•	Real time performance is critical
	⁃	Replaying million events to get state may slow down reads unless we build snapshots — this add complexities.

ASK YOURSELF
{\rtf1\ansi\deff0
\trowd
\cellx1000
\cellx2000
\intbl Criteria\cell
\intbl Ask Yourself\cell
\row
\trowd
\cellx1000
\cellx2000
\intbl Audability\cell
\intbl Do I need to know how this state came to be?\cell
\row
\trowd
\cellx1000
\cellx2000
\intbl Debugging\cell
\intbl Will I want to rewind or re-simulate the system?\cell
\row
\trowd
\cellx1000
\cellx2000
\intbl Regulatory\cell
\intbl Will legal/compliance require a full trace?\cell
\row
\trowd
\cellx1000
\cellx2000
\intbl Scalability\cell
\intbl Will decoupled microservices benefits from events?\cell
\row
\trowd
\cellx1000
\cellx2000
\intbl Collaboration\cell
\intbl Do users work on same data and need history/undo?\cell
\row
\trowd
\cellx1000
\cellx2000
\intbl Change Frequency\cell
\intbl Will my business logic change often enough to play things differently?\cell
\row
} 
